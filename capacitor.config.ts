import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tum.hackyard',
  appName: 'HackYard',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
